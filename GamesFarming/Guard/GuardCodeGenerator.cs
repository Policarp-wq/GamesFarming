using System;
using System.Buffers.Text;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace GamesFarming.Guard
{
    public static class GuardCodeGenerator
    {
        public static string GetCode(string sharedSecret)
        {
            var acc = new Stea
        }
        public static string DecodeFrom64(string encodedData)
        {
            return Convert.F System.Text.Encoding.UTF8.GetBytes(encodedData).ToString();
        }
    }
}
